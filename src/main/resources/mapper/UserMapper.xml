<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demoproject.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.example.demoproject.entity.User">
        insert into
            user(username, password, company_name, phone)
        values
            (#{username}, #{password}, #{companyName}, #{phone})
    </insert>

    <select id="selectUserByPhone" resultType="com.example.demoproject.entity.User">
        select
            user_id as userId, username, company_name as companyName, phone, password
        from
            user
        where
            phone = #{phone}
    </select>
</mapper>